# WebManager.id

Dependency-free bilingual static landing page for website development and managed website services. Defaults to Bahasa Indonesia and persists the selected language in `localStorage` (`wm-lang`).
